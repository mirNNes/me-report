build:
    image: default-bionic

    nodes:
        analysis:
            environment:
                php:
                    version: 8.2
                node:
                    version: 16
            cache:
                directories:
                    - report/vendor/
                    - report/node_modules/
            commands:
                - cd report
                - ls -la
                - npm ci || npm install
                - ./vendor/bin/phpunit --coverage-clover=docs/coverage.clover || true
                - php-scrutinizer-run || true
