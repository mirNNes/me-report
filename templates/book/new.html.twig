{% extends 'base.html.twig' %}

{% block title %}Ny bok{% endblock %}

{% block body %}
    <div class="site-content">
        <h1>Skapa ny bok</h1>

        {{ include('book/_form.html.twig', { button_label: 'Spara' }) }}

        <div class="actions">
            <a href="{{ path('app_book_list') }}">⬅ Tillbaka</a>
        </div>
    </div>
{% endblock %}
