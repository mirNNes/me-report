<div class="site-content" style="max-width: 600px; margin: 0 auto; padding: 2rem;">
    {{ form_start(form) }}
        {{ form_widget(form) }}

        <div class="actions" style="text-align: center; margin-top: 1rem;">
            <button class="btn btn-primary" style="padding: 0.5rem 1.2rem; font-size: 1rem; border-radius: 5px;">
                {{ button_label|default('Spara') }}
            </button>
        </div>
    {{ form_end(form) }}
</div>
